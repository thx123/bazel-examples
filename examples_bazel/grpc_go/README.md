To watch the gRPC server and client in action, do the following:

1. Start the gRPC server:
```
bazel run server
```

2. Run the gRPC client:
```
bazel run client
```

3. Observe the gRPC server log changes on the CLI.
